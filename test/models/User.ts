export interface User {
  // Text Box form fields (TC02)
  fullName?: string;
  email?: string;

  // Practice Form fields (TC03)
  firstName?: string;
  lastName?: string;
  gender?: string;
  mobile?: string;
  currentAddress?: string;
}
